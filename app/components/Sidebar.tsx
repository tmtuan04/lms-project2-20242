import HustLogo from "./HustLogo";

const Sidebar = () => {
  return (
    <header className="flex justify-between items-center p-3 shadow-md bg-white sticky">
      <HustLogo></HustLogo>
    </header>
  );
};

export default Sidebar;
